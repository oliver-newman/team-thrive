<% provide(:title, "Edit User") %>
<h1>Update Your Profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="gravatar-edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails" target="_blank">change</a>
    </div>

    <%= form_for(@user, html: { class: "edit-form" } ) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :fundraising_goal %>
      <%= f.number_field :fundraising_goal, class: 'form-control' %>

      <%= f.label :first_name %>
      <%= f.text_field :first_name, class: 'form-control' %>

      <%= f.label :last_name %>
      <%= f.text_field :last_name, class: 'form-control' %>

      <%= f.label :unit_preference %></br>
      <div class="radio">
        <label>
          <%= f.radio_button :unit_preference, "feet" %>Feet/Miles
        </label>
      </div>
      <div class="radio">
        <label>
          <%= f.radio_button :unit_preference, "meters" %>Meters/Kilometers
        </label></br>
      </div>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.submit "Save Changes", class: "btn btn-theme" %>
    <% end %>

  </div>
</div>
